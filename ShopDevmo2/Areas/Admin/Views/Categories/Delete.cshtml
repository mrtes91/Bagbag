@model ShopDevmo2.Models.Category

@{
    ViewBag.Title = "Delete";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>Delete</h2>

<h3 class="font-weight-bold">Are you sure you want to delete this?</h3>
<div>
    <h4>Category</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Parent.DisplayText)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Parent.DisplayText)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.DisplayText)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.DisplayText)
        </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-danger rounded-0 font-weight-bold mt-3" />
            <a target="_self" href="@Url.Action("Index", "Categories")" class="btn btn-secondary rounded-0 font-weight-bold mt-3">Back to List</a>
            @*<input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")*@
        </div>
    }
</div>
